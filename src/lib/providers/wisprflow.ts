/**
 * REVERTED 2026-07-12, same day it was added — do not import this file.
 *
 * Wispr Flow's Voice Interface API turned out to be invite-only (no
 * self-serve signup; requires enterprise@wisprflow.ai approval), which is
 * incompatible with the launch timeline. The walk's speech-to-text is back
 * on ElevenLabs Scribe — see src/lib/stt.ts.
 *
 * This file is intentionally left as dead code rather than force-deleted
 * (the sandbox couldn't remove it directly). Safe to delete by hand, or
 * revisit if Wispr Flow access is ever approved and still wanted.
 */
export {};
