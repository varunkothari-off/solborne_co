---
/**
 * LEGACY ROUTE — the rule-based diagnostic is superseded by the voice-first
 * walk. Old links and bookmarks land here; send them onward permanently.
 * (The full old implementation is preserved in git history if ever needed.)
 */
import { walk } from '../data/walk';

export const prerender = false;

return Astro.redirect(walk.path, 301);
---
